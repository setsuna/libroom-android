package com.waseem.libroom.feature.auth.presentation

import androidx.compose.runtime.Composable

@Composable
fun AuthorizationScreen(viewModel: AuthorizationViewModel, onAuthorized: () -> Unit) {
    // Implement the UI for device authorization
    // This screen should allow the user to input necessary information for device authorization
    // Once authorized, call onAuthorized()
}